One-line: square 16px checkbox with optional description line; supports an indeterminate group state.

```jsx
<Checkbox label="Delete source after transfer" description="Verified checksum required before deletion." checked={v} onChange={...} />
<Checkbox label="All clips" indeterminate />
```

Use for independent options and multi-select tables. For an immediate on/off setting use Switch.
